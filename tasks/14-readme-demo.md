# 14 — README + Demo

## Outputs
- README.md polish (already drafted — refine after the code lands).
- An animated demo GIF or static screenshot under `docs/demo.gif` (optional; skip if no recorder).

## Steps
1. Re-read `README.md`; ensure every code block actually runs against the current code.
2. Add a "Status" badge section reflecting reality.
3. Run `01_quickstart.ipynb`; screenshot the silver DataFrame; save under `docs/`.
4. Update the "Backends" table if anything has changed.

## DoD
- All `README.md` code blocks have been actually run.
- Demo asset committed.
- `doc-scribe` marks all tasks complete in `runtime/PROGRESS.md`.
